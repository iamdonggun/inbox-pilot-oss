# 관찰 기록 — shadow 동결 기간

**기간 시작: 2026-08-17** · Stage 2 게이트까지 `tier0.py` 규칙 동결
(README §7 「규칙 동결」)

이 기간에 발견한 오분류는 **고치지 않고 여기에 누적합니다.** 게이트 조건이
"2주 무사고"인데 분류기가 계속 바뀌면 측정 대상이 같이 움직여 측정 자체가
무의미해집니다. 관찰 기간의 요건은 정확도가 아니라 **고정**입니다.

**유일한 예외는 P0 recall 실패(중요 메일을 놓친 것)** — 즉시 수정하고 아래
「즉시 수정한 건」에 남깁니다. 그 외 정밀도 문제는 전부 기록만 합니다.

---

## ⚠️ 이 파일은 추적됩니다 — 무엇을 적을 수 있는가

이 저장소는 공개될 수 있다는 전제이며(README 하드룰), 이 파일은 `out/`이
아니라 저장소 루트에 있어 **커밋됩니다.** 게이트 판단의 입력이 백업 없는
곳에 있으면 안 되기 때문입니다. 그 대가로 적을 수 있는 것이 제한됩니다.

| 적는다 | 적지 않는다 |
| --- | --- |
| `message_id` (Gmail 메시지 id) | 발신자 주소 · 수신자 주소 |
| 계정 키 (`main` / `dev`) | 제목 · 본문 · 본문 인용 |
| `urgency` / `category` / `rule` (`audit.jsonl` 그대로) | 사람·회사 이름 |
| 규칙 동작에 대한 설명 | 메일 내용에 대한 설명 |

`rule` 값에 들어가는 발신 **도메인**(`infra-domain:github.com`,
`personal-sender:naver.com`)은 적어도 됩니다 — 이미 `config.py`에 공개돼
있는 목록이고, 도메인은 개인을 식별하지 않습니다.

**「근거」 칸은 메일 내용이 아니라 규칙 동작으로 씁니다.** "거래처 견적
요청이라 P1이어야 함"이 아니라 "규칙 8이 소비자 도메인 조건만으로
통과시킴 — 자동발송 여부를 보지 않음". 내용을 확인해야 하면 `message_id`로
메일함에서 직접 엽니다. 그것이 이 id를 적는 이유입니다.

---

## 구조적 관찰 — 특정 메일이 아니라 규칙의 성질

동결 대상이라 지금 고치지 않습니다. 게이트 후 일괄 재검토합니다.

### O-1 · 2026-08-17 · 신뢰 목록이 발신자 한 곳뿐

`config.SECURITY_ALERT_DOMAINS`에 `accounts.google.com` 하나만 있습니다.
규칙 2(보안 경고 P0)는 이 목록에 있는 발신자에게만 발화하므로, 다른
발신자의 **진짜 침해 경고는 P0가 아니라 P1**(규칙 6, 접근 통지)로
갑니다. 실측 창에서 `megazone.com`·`navercorp.com` 발신 접근 통지 3건이
그렇게 P1에 있습니다.

- **영향**: P0 recall 문제로 보이지만, 놓치는 것이 아니라 한 단계 낮게
  올라오는 것입니다. 다이제스트 최상단 「즉시 확인 — P0/P1」 블록에 함께
  노출되므로 **사람 눈에서 사라지지는 않습니다.** 그래서 즉시 수정 예외에
  해당하지 않는다고 판단했습니다.
- **재검토할 것**: 목록을 넓힐지, 아니면 규칙 6의 P1을 그대로 둘지. 넓히면
  그 발신자의 저신호 메일이 P0로 새는 문제가 따라옵니다 — 목록에 도메인을
  추가할 때는 그 발신자가 **그 밖에** 무엇을 보내는지 함께 봐야 합니다
  (규칙 2가 도메인 단독으로 승격하지 않는 이유).
- **동결 상태**: 목록 변경 = 규칙 변경 → 게이트까지 손대지 않음.
- **중간 관찰 (2026-08-29)**: 관찰 기간 12일 174건에서 규칙 2는 **0회**
  발화했습니다. 신뢰 목록 발신자의 보안 경고가 창 안에 한 건도 없었기
  때문입니다. 즉 O-1은 이 창에서 재현되지도, 반증되지도 않았습니다 —
  "무사고"가 규칙 2에 대한 증거가 아니라는 뜻입니다.

### O-2 · 2026-08-17 · 규칙 8(개인 발신)의 오탐 위험 — 미실측

규칙 8은 Gmail `personal` 카테고리 ∧ `config.CONSUMER_DOMAINS` ∧
`List-Unsubscribe` 없음 세 조건만 봅니다. **자동발송 여부는 보지 않습니다.**
`gmail.com` 주소로 발송하는 소상공인 자동메일(주문 확인 등)이 이 세 조건을
모두 통과하면 `P2` + `needs_reply=true`로 올라옵니다.

- **실측 상태**: 2026-08-05~17 창 162건에 **사례 0건**. 이 창에서 규칙 8이
  잡은 4건은 전부 `naver.com` 발신의 사람이 쓴 메일이었습니다. 위험은
  추론이지 관측이 아닙니다.
- **기록할 것**: 관찰 기간 중 발생하면 아래 「개별 오분류」에 `message_id`와
  함께 남깁니다. 반복되면 게이트 후 `no-?reply` 발신자 제외 조건을 검토할
  근거가 됩니다. 1~2건이면 감수하는 쪽이 맞습니다 — 비대칭 튜닝 원칙상
  사람이 쓴 메일을 놓치는 비용이 더 큽니다.
- **동결 상태**: 조건 추가 = 규칙 변경 → 게이트까지 손대지 않음.
- **중간 관찰 (2026-08-29)**: 규칙 8도 12일 174건에서 **0회** 발화했습니다.
  기준선 창에서 4건을 잡던 규칙입니다. O-2가 말한 오탐 위험은 여전히
  미실측이고, 이제는 규칙 자체가 미실행 상태라는 사실이 하나 더
  붙습니다. 규칙 5·6·7도 같습니다(아래 O-3).

---

### O-3 · 2026-08-29 · 승격 규칙 6종이 12일간 한 번도 발화하지 않음

관찰 기간 174건의 `rule` 분포입니다. 규칙 2(보안 경고 P0), 3(VIP 기관 P0),
5(VIP 개인 P1), 6(접근 통지 P1), 7(문의 폼 리드 P1), 8(개인 발신 P2) —
**사람·기관 신호를 올리는 규칙 전부가 0회**입니다.

| 발화한 규칙 | 건수 |
| --- | --- |
| `list-unsubscribe-header` (11) | 107 |
| `receipt-keyword` (10) | 28 |
| `gmail-category:updates` (12) | 21 |
| `claude-haiku-4-5` (Tier 1) | 7 |
| `infra-domain:*` (4) | 4 |
| `receipt-over-infra:*` (4 예외) | 2 |
| `otp-pattern` (1) | 2 |
| `gmail-category:promotions` (12) | 2 |
| `action-required` (9) | 1 |

- **영향**: 게이트 조건 "2주 무사고"의 해석에 직접 걸립니다. 발화하지 않은
  규칙에 대해 무사고는 **증거가 아닙니다.** 이 창이 실제로 검증한 것은
  규칙 4(인프라)·9(조치 요구)·10(영수증)·11(대량 발송)·12(Gmail 카테고리)와
  Tier 1 폴백 경로뿐입니다.
- **재검토할 것**: 게이트를 "2주 무사고"만으로 통과시킬지, "승격 규칙이 각각
  최소 1회 발화하고 그 판정이 옳았다"를 추가할지. 전자만 쓰면 조용한 메일함이
  좋은 분류기로 오독됩니다.
- **동결 상태**: 관찰만. 규칙 변경 아님.

### O-4 · 2026-08-29 · 규칙 10이 본문을 읽고 규칙 11보다 앞서 실행됨

이 창에서 가장 많이 잘못 판정한 지점입니다. 규칙 10(`RECEIPT_PATTERN`)은
`_keyword_haystack` = **제목 + 본문**을 봅니다. 승격 규칙 2·6·9가 제목만
보는 것과 다릅니다. 그리고 규칙 11(List-Unsubscribe → P5)보다 **앞**에
있습니다 — 진짜 결제 알림에 해당 헤더가 붙는 사례(토스페이먼츠) 때문에
의도적으로 그렇게 둔 순서입니다(`tier0.py` 규칙 11 주석).

두 성질이 합쳐지면 이렇게 됩니다.

- 본문에 결제 어휘가 상시 들어가는 발신자의 **모든** 메일이, 제목에 영수증
  어휘가 하나도 없어도 영수증이 됩니다.
- `List-Unsubscribe` + Gmail `promotions` 를 **둘 다** 가진 메일이, 본문이
  규칙 10에 걸리는 순간 P5로 내려가지 못하고 P3에 남습니다.

관찰 기간 P3 21건(중복 제거) 중 **8건이 영수증이 아닙니다**. 정밀도 손실
약 38%. 그중 7건이 이 규칙 때문이고(M-1~M-7), 나머지 1건은 규칙 9가 표현
차이로 빠진 결과입니다(M-8, 아래 O-5).

- **영향**: 정밀도 문제입니다. 놓치는 것이 아니라 영수증 더미가 오염되는
  것이고, 다이제스트 「영수증 / 결제」 섹션의 신뢰도가 떨어집니다.
  즉시 수정 예외에 해당하지 않습니다.
- **재검토할 것**: (a) 규칙 10을 제목 우선으로 좁힐지 — 단, 본문에만
  금액이 있는 진짜 영수증을 놓칠 수 있으므로 실측 없이 바꾸지 않습니다.
  (b) `promotions` 카테고리 + `List-Unsubscribe` 를 **둘 다** 가진 메일에
  한해 규칙 11을 규칙 10보다 앞에 둘지. 토스페이먼츠 사례는 `promotions`가
  아니었으므로 이 조합이면 살아남습니다 — 게이트 후 실측으로 확인할 것.
- **주의**: 이 프로젝트에서 가장 많이 재발한 버그 계열(일반 키워드가 구체
  신호를 삼킴)의 여섯 번째 사례입니다. 고칠 때 반드시 충돌 케이스를 먼저
  적고 시작합니다.
- **동결 상태**: 관찰만.

### O-5 · 2026-08-29 · 규칙 9는 발신자가 쓴 표현을 그대로만 받음

`ACTION_REQUIRED_PATTERN`은 "action required" / "조치 필요" / "종료 안내"
같은 **고정 표현**만 봅니다. 창 안에서 두 건이 표현 차이 하나로 빠졌습니다
(M-8, M-9): 하나는 "종료 안내" 대신 "종료 예정일 변경 안내", 다른 하나는
"Action Required" 대신 "Action Advised". 둘 다 `List-Unsubscribe`가 없어
규칙 9의 게이트는 통과했고, 패턴만 어긋났습니다.

- **영향**: M-8은 규칙 9가 잡으라고 만들어진 바로 그 형태입니다 — 발신자가
  제목에 종료·마감을 명시했고 `List-Unsubscribe`도 없습니다. P2여야 할 것이
  P3로 갔습니다. 다이제스트 「영수증 / 결제」 섹션에는
  올라오므로 눈에서 사라지지는 않습니다 — 그래서 즉시 수정 예외로 보지
  않았습니다. 다만 이 창에서 **가장 비용이 큰 오분류**입니다.
- **재검토할 것**: 패턴을 넓히는 방향은 위험합니다. README §4가 기록한 대로
  마감 문구는 광고의 관용구입니다("(광고) 마감 D-1"). 표현을 늘리는 대신
  규칙 9의 위치를 규칙 10 **앞**으로 유지한 채 어휘만 두 개
  ("변경 안내", "Action Advised") 추가하는 최소 변경이 후보입니다.
  추가 전에 그 어휘가 광고에서 무엇을 뜻하는지 실측할 것.
- **동결 상태**: 관찰만.

### O-6 · 2026-08-29 · 비대칭 튜닝이 Tier 1에는 구현되어 있지 않음 (문서/코드 불일치)

README §6은 "확신이 낮으면 더 높은 우선순위로 올립니다"를 분류 전체의
원칙으로 적어 두었습니다. 그런데 이 원칙은 **Tier 0에만** 존재합니다.

- `tier1.SYSTEM_PROMPT`에는 확신이 낮을 때 승격하라는 지시가 없습니다.
  confidence는 보고만 하게 되어 있습니다.
- 반환된 confidence로 urgency를 올리는 코드도 없습니다. `digest.py`는
  같은 우선순위 안의 **정렬 키**로만 씁니다.

측정된 사례: M-10은 Tier 1이 confidence 0.65로 P5(최하위)로 보냈습니다.
원칙대로면 확신이 낮을수록 위로 가야 하는데, 실제로는 그 판정이 가장 안
보이는 곳으로 갔습니다.

- **영향**: Tier 1은 창의 4.0%(7건)뿐이라 지금은 규모가 작습니다. 다만
  문서가 코드보다 강하게 적혀 있는 상태이므로, 문서를 믿고 다음 규칙을
  설계하면 어긋납니다.
- **동결 대상인가**: Tier 0 판정 로직이 아니므로 문언상 동결 대상은
  아닙니다. 그러나 판정을 바꾸는 변경이고, 동결의 취지는 "측정 대상을
  움직이지 않는 것"이므로 게이트까지 손대지 않습니다. 지금 할 수 있는 것은
  README §6에 "현재 Tier 0에만 적용됨"을 명시하는 문서 수정뿐입니다.
- **재검토할 것**: (a) 프롬프트에 승격 지시를 넣을지, (b) 코드에서
  confidence 임계값 아래를 한 단계 올릴지, (c) 원칙을 Tier 0 한정으로
  좁혀 적을지.

### O-7 · 2026-08-29 · 같은 발신자의 같은 성격 메일에 Tier 1 판정이 갈림

관찰 기간 안: M-11(정기 점검 공지) → **P1** / infra / confidence 0.95.
동결 이전(2026-08-15 창): 같은 발신자의 같은 성격 공지가 → **P4** / update /
confidence 0.95. 서로 다른 메일이므로 동일 입력의 비결정성은 아닙니다.
그러나 사람이 보면 같은 종류이고, 양쪽 다 0.95를 붙였습니다.

- **영향**: Tier 1이 규칙 4가 비워 둔 자리를 메우고 있고, 그 결과가 창마다
  세 단계씩 흔들립니다. README §4의 "카테고리가 반복되면 규칙으로 옮긴다"에
  정확히 해당합니다.
- **재검토할 것**: 이 발신 도메인을 `INFRA_DOMAINS`에 넣을지. 넣기 전에
  그 발신자가 **그 밖에** 무엇을 보내는지 확인할 것(규칙 2가 도메인 단독으로
  승격하지 않는 이유와 같습니다).
- **동결 상태**: `INFRA_DOMAINS` 변경 = 규칙 변경 → 게이트까지 손대지 않음.


## 개별 오분류 — 기록만 (정밀도 문제)

「현재 판정」은 `audit.jsonl` 그대로입니다. 「근거」는 규칙 동작으로만 씁니다 —
내용을 봐야 하면 `message_id`로 메일함에서 엽니다.

| # | 날짜 | 계정 | message_id | 현재 판정 | 기대 판정 | 근거 (규칙 동작으로) |
| --- | --- | --- | --- | --- | --- | --- |
| M-1 | 2026-08-27 | main | `1a046259f0ee5d27` | P3 / receipt / `receipt-keyword` | P5 / promo | `List-Unsubscribe` **있음** + Gmail `promotions`. 그런데 규칙 10이 규칙 11보다 앞이고 본문까지 읽어, 판촉 본문의 구매 문구가 먼저 걸림 (O-4) |
| M-2 | 2026-08-28 | main | `1a04800e4650e17f` | P3 / receipt / `receipt-keyword` | P5 / promo | M-1과 동일. 헤더·카테고리 둘 다 광고를 가리키는데 규칙 10이 선행 |
| M-3 | 2026-08-28 | main | `1a0486e8e802189a` | P3 / receipt / `receipt-keyword` | P4 / update | 헤더 없음·`updates` 카테고리. 규칙 10이 본문 상용구를 잡아 규칙 12까지 가지 못함 |
| M-4 | 2026-08-21 | main | `1a020912a1d118ca` | P3 / receipt / `receipt-keyword` | P4 / update | M-3과 동일. 같은 성격의 메일이 2026-08-18에도 있었음 (`1a0144f52606d786`) |
| M-5 | 2026-08-20 | main | `1a01f45ca7c14752` | P3 / receipt / `receipt-keyword` | P4 / update | 제목에는 영수증 어휘가 없음. 규칙 10이 본문에서만 걸렸고, 제목만 봤다면 규칙 12로 갔음 |
| M-6 | 2026-08-20 | main | `1a01f370bb8a71bb` | P3 / receipt / `receipt-keyword` | P4 / update | M-5와 동일 발신·동일 형태 |
| M-7 | 2026-08-21 | dev | `1a0229bc6caf5cc3` | P3 / receipt / `receipt-keyword` | P4 (또는 P2) | M-5와 같음 — 제목에는 영수증 어휘가 없고 본문 상용구만으로 규칙 10이 발화. 이 발신자의 메일은 전부 같은 경로를 탐 |
| M-8 | 2026-08-28 | main | `1a0475587a285212` | P3 / receipt / `receipt-keyword` | **P2 / action** | 헤더 없음 → 규칙 9의 게이트는 통과. `ACTION_REQUIRED_PATTERN`의 「종료 안내」와 제목의 표현이 한 어절 어긋나 미발화, 규칙 10으로 흘러감 (O-5). **이 창에서 비용이 가장 큰 오분류** |
| M-9 | 2026-08-26 | dev | `1a0407f814d0768f` | P4 / update / `gmail-category:updates` | P2 / action | 규칙 9가 「Action Required」만 알고 「Action Advised」를 모름. 같은 메일이 하루 2회 도착 (`1a03d97d603f197e`) |
| M-10 | 2026-08-27 | main | `1a0429c6f3a742ff` | P5 / other / `claude-haiku-4-5` (conf 0.65) | 판단 보류 | Tier 1이 창에서 가장 낮은 확신으로 **가장 낮은** 우선순위를 매김. 비대칭 튜닝 원칙과 반대 방향 (O-6) |
| M-11 | 2026-08-25 | main | `1a037d289998e645` | P1 / infra / `claude-haiku-4-5` (conf 0.95) | 규칙화 대상 | 규칙 4가 비운 자리를 Tier 1이 메움. 동결 이전 같은 발신자의 같은 성격 공지는 P4/update/0.95였음 (O-7) |

### 설계상 의도된 판정 — 오분류 아님, 재검토용으로만 기록

게이트 후 O-1·O-2와 함께 볼 때 필요한 반례들입니다. 지금 고칠 대상이
아닙니다.

| 날짜 | 계정 | message_id | 판정 | 왜 의도적인가 |
| --- | --- | --- | --- | --- |
| 2026-08-20 | main | `1a01cebfc7029928` | P4 / update | 비밀번호 관련 통지지만 발신자가 신뢰 목록 밖. 규칙 6이 침해 **주장** 문구를 일부러 제외함 — 목록 밖에서 그 문구의 전형은 피싱 |
| 2026-08-20 | main | `1a01ced09876646b` | P4 / update | 위와 동일. 계정 변경 통지이나 「로그인」이 아니어서 `LOGIN_ALERT_PATTERN` 대상 아님 |
| 2026-08-27 | main | `1a0429bab6816f2b` | P4 / update | 규칙 1은 코드를 가리키는 **명사구**만 봄. 동사(verify·activate)를 넣으면 조치 요구 메일까지 Ephemeral로 삼킴 |
| 2026-08-27 | main | `1a0428f5135331a1` | P4 / update | 위와 동일 |

## 즉시 수정한 건 — P0 recall 실패

이 표가 비어 있는 것이 "2주 무사고"의 정의입니다.

| 날짜 | 계정 | message_id | 놓친 이유 | 수정 커밋 |
| --- | --- | --- | --- | --- |
| | | | | |

### 2026-08-29 중간 점검 — 무엇을 근거로 비어 있다고 하는가

관찰 기간 174건 전부를 훑었고, P3/P4/P5로 내려간 판정 중 P0에 해당하는
것은 없었습니다. 그러나 **이 확인에는 한계가 있습니다.**

- 판단 근거는 다이제스트의 **제목·발신자와 `audit.jsonl`의 판정**뿐입니다.
  본문은 보지 않았습니다. 제목이 무해한데 본문이 P0인 메일은 이 방법으로
  걸러지지 않습니다.
- 규칙 2·3·5·6·7·8이 0회 발화했으므로(O-3), 이 창은 그 규칙들의 recall에
  대해 아무것도 말해 주지 않습니다. "P0를 놓치지 않았다"와 "P0 규칙이
  작동한다"는 다른 문장이고, 이 창이 뒷받침하는 것은 앞의 것뿐입니다.

---

## 기간 종료 시 확인할 것

기간 종료: **2026-08-31** (동결 시작 2026-08-17 + 2주)

- [ ] 「즉시 수정한 건」이 비어 있는가 (= 무사고 2주가 실제로 채워졌는가)
- [ ] 「개별 오분류」를 묶어서 한 번에 검토 — 개별 대응이 아니라 패턴으로
- [ ] O-1 (신뢰 목록 확대 여부) 재검토 — **창에서 규칙 2가 0회 발화**했음을 전제로
- [ ] O-2 (규칙 8 자동발송 제외 조건) — 관측 사례가 쌓였는지 확인 후 판단
- [ ] O-3 — 게이트 조건을 "2주 무사고"만으로 둘지, "승격 규칙 각각 1회 이상
      발화 + 그 판정이 옳았음"을 추가할지 **먼저** 결정. 이것을 정하지 않으면
      나머지 항목의 통과 여부를 해석할 수 없음
- [ ] O-4 (규칙 10의 본문 읽기 + 규칙 11과의 순서) — 이 창 오분류의 다수
- [ ] O-5 (규칙 9 어휘 2종 추가) — 추가 전 그 어휘가 광고에서 무엇을 뜻하는지 실측
- [ ] O-6 (비대칭 튜닝 Tier 1 미적용) — 코드를 고칠지 문서를 좁힐지 결정
- [ ] O-7 (반복되는 카테고리를 규칙으로) — `INFRA_DOMAINS` 추가 여부

## 중간 집계 (2026-08-29, 관찰 12일차 174건)

기준선과 같은 12일 길이입니다. 오분류 판단 전에 이 표를 먼저 봅니다 —
분포가 크게 움직였으면 규칙이 아니라 메일 구성이 바뀐 것입니다.

| 항목 | 기준선 (08-05~17, 162건) | 관찰 창 (08-17~29, 174건) |
| --- | --- | --- |
| 분류 경로 | Tier0 159 (98.1%) / Tier1 3 (1.9%) | Tier0 167 (96.0%) / Tier1 7 (4.0%) |
| P0 | 7 | 4 |
| P1 | 6 | 1 |
| P2 | 8 | 3 |
| P3 | 26 | 30 |
| P4 | 27 | 23 |
| P5 | 86 | 111 |
| Ephemeral | 2 | 2 |
| 이 창의 Tier1 비용 | $0.0042 | $0.0103 |
| 계정별 | — | main 156 / dev 18 |

**읽는 법.** 총량은 거의 같은데 상위(P0+P1+P2)가 21 → 8로 줄고 P5가
86 → 111로 늘었습니다. 규칙이 나빠진 것이 아니라 **승격 규칙이 걸릴 메일
자체가 오지 않은 창**입니다(O-3: 규칙 2·3·5·6·7·8 전부 0회). Tier 1
비율이 2배가 된 것도 같은 방향의 신호입니다 — 규칙이 아는 형태에서
벗어난 메일이 늘었다는 뜻입니다.

따라서 이 창의 "무사고"는 **분류기의 품질보다 메일 구성에 더 많이
빚지고 있습니다.** 게이트 판단에서 이 사실을 빼면 안 됩니다.

## 시작 시점의 기준선 (2026-08-17, 12일 창 162건)

동결 시작 상태입니다. 관찰 기간 중 분포가 크게 움직이면 규칙이 아니라
메일 구성이 바뀐 것이므로, 오분류 판단 전에 이 표와 먼저 비교합니다.

| 항목 | 값 |
| --- | --- |
| 분류 경로 | Tier0 159 (98.1%) / Tier1 3 (1.9%) |
| 우선순위 | P0 7, P1 6, P2 8, P3 26, P4 27, P5 86, Ephemeral 2 |
| 이 실행 비용 | $0.0042 USD |
