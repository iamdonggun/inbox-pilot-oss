# inbox-pilot 환경변수 예시. 실제 값은 .env 에 넣습니다(.gitignore 대상).
# 형식은 한 줄 KEY=VALUE — config.load_env_file() 이 읽습니다.

# Tier 1 (Anthropic). 없으면 Tier1 이 비활성화되고 Tier0 만 돕니다.
ANTHROPIC_API_KEY=

# 실패·복구 알림(notify.py). 없으면 알림만 건너뛰고 파이프라인은 정상 동작합니다.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
